import { test } from 'naive-tests';
import { GameWeb } from '../../src/game-web/game-web.js';

test('game - tick', () => {
    const renderer = {
        tex: function () { },
        clear: function () { },
        draw: function () { }
    };
    //const game = new GameWeb(renderer);
    //game.tick();
});