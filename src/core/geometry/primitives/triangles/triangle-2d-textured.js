import { AxisAlignedBoundingBox2d } from "../boxes/axis-aligned-bounding-box-2d.js";

export function drawTriangle2dTexuredLeftToRight(targetTex, triangle, tex, offset) {
    //const aabb = BoundingBox2d.createFromPoints2d(triangl);
    //const center = aabb.center();
}